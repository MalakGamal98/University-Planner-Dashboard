import { Card, CardDescription, CardHeader, CardTitle } from '@/components/ui'

export function SettingsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Settings</CardTitle>
        <CardDescription>
          App preferences and configuration will be built in a future phase.
        </CardDescription>
      </CardHeader>
    </Card>
  )
}
